import { SlideToggleDirective } from './slide-toggle.directive';

describe('SlideToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new SlideToggleDirective();
    expect(directive).toBeTruthy();
  });
});
